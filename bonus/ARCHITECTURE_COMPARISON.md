# 📚 Library Management System - Architecture Upgrade

เอกสารสรุปการปรับปรุงสถาปัตยกรรมระบบจาก **Layered Architecture** สู่ **Client-Server Architecture**

---

## 🏗️ Architecture Comparison

### 1. Layered Architecture (Before)
*การทำงานแบบเดิมที่ทุกส่วน (Frontend & Backend) รวมอยู่ในก้อนเดียวกัน*

**Pros:**
* **Simplicity:** พัฒนาและทดสอบได้ง่ายในช่วงเริ่มต้น ไม่ต้องจัดการเรื่อง Network
* **Performance:** การสื่อสารระหว่าง Layer ทำได้รวดเร็วผ่าน Memory
* **Data Integrity:** จัดการความถูกต้องของข้อมูลได้ง่ายเพราะมีศูนย์กลางเดียว

**Cons:**
* **Tight Coupling:** ส่วนต่างๆ ยึดติดกันมากเกินไป ยากต่อการเปลี่ยนเทคโนโลยีเฉพาะส่วน
* **Scalability:** ขยายระบบได้ยาก ต้องขยายทั้งโปรเจกต์พร้อมกัน
* **Deployment:** แก้ไขจุดเล็กน้อยต้อง Re-deploy ระบบใหม่ทั้งหมด

### 2. Client-Server Architecture (After)
*สถาปัตยกรรมปัจจุบันที่แยกส่วนหน้าบ้านและหลังบ้านออกจากกัน*

**Pros:**
* **Decoupling:** แยกหน้าที่ชัดเจน (Separation of Concerns) ทำให้ทีมพัฒนาทำงานขนานกันได้
* **Flexibility:** รองรับการเปลี่ยน Client ได้หลากหลาย (เช่น Web, Mobile) โดยใช้ Backend เดิม
* **Scalability:** สามารถเลือกขยายระบบเฉพาะส่วนที่มีการใช้งานหนักได้อิสระ

**Cons:**
* **Complexity:** เพิ่มความซับซ้อนในการจัดการระบบ เช่น CORS และ Network Latency
* **Overhead:** มีภาระเพิ่มขึ้นจากการส่งข้อมูลผ่าน HTTP/JSON

---

## 🛠️ Changes Made (การเปลี่ยนแปลงที่เกิดขึ้น)

### 1. Separation
* **แยก Frontend และ Backend:** แบ่งเป็น 2 โปรเจกต์อิสระ เพื่อให้ทำงานแยกกันได้ชัดเจน

### 2. Communication
* **REST API (HTTP/JSON):** เปลี่ยนจากการเรียกใช้ฟังก์ชันตรงๆ เป็นการสื่อสารผ่าน JSON บนโปรโตคอล HTTP

### 3. CORS (Cross-Origin Resource Sharing)
* **CORS Middleware:** เพิ่มการตั้งค่าฝั่ง Backend เพื่ออนุญาตให้ Client จาก Domain อื่นสามารถเข้าถึง API ได้อย่างปลอดภัย

### 4. API Response Format
* **Standardization:** กำหนดรูปแบบการตอบกลับให้เป็นมาตรฐานเดียวกัน เช่น `{ success, data, timestamp }` เพื่อให้ง่ายต่อการประมวลผล

---

## 🚀 How to Run
1.  **Backend:** `cd backend` -> `npm start` (Running on Port 3000)
2.  **Frontend:** `cd frontend` -> `python -m http.server 8000` (Running on Port 8000)